{
  "brokerMode": "proxy",
  "brokerProxyUrl": "wss://example.com/mosquitto/",
  "brokerDirectUrl": "ws://localhost:9001",

  "responderBaseUrl": "http://localhost:8081",

  "username": "{{ mqtt_client_user }}",
  "password": "{{ mqtt_client_password }}",

  "diaries": "diaries",
  "files": "files",
  "clientId": "client",
  "keepalive": 60,
  "reconnectPeriod": 1000,
  "connectTimeout": 30000,
  "protocolVersion": 5,
  "clean": false
}
