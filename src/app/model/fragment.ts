import { Marquee } from "./marquee";

export class Fragment {
  constructor(
    id: number,
    year: number,
    month: number,
    day: number,
    sequence: number,
    marquee: Marquee,
    text: string
  ) {};
}
